# Pi-Guard Lite 工作状态清单

更新时间：2026-03-30

## 1. 已完成

### 1.1 方案与文档
- 已生成总体方案文档
- 已生成从 0 到落地详细步骤文档
- 已生成开发视角实施任务分解文档

### 1.2 项目工程骨架
- 已创建项目根目录：`E:\ctf\开发\pi-guard-lite`
- 已创建目录：
  - `app/`
  - `config/`
  - `models/`
  - `scripts/`
  - `systemd/`
  - `data/`
  - `logs/`
  - `tests/`
  - `docs/`
  - `templates/`
  - `static/`
- 已创建 Python 包初始化文件 `__init__.py`

### 1.3 基础工程文件
- 已创建 `README.md`
- 已创建 `.gitignore`
- 已创建 `requirements.txt`

### 1.4 配置文件
- 已创建 `config/app.yaml`
- 已创建 `config/assets.yaml`
- 已创建 `config/whitelist.yaml`
- 已创建 `config/executor.yaml`

### 1.5 数据层基础
- 已创建 SQLite 初始化脚本：`scripts/init_db.sql`
- 已定义核心表：
  - `asset`
  - `event`
  - `policy`
  - `blocklist`
  - `probe_result`
  - `model_version`
  - `audit_log`

### 1.6 后端基础代码
- 已创建配置加载模块：`app/common/config.py`
- 已创建日志模块：`app/common/logger.py`
- 已创建数据库模块：`app/common/db.py`
- 已创建基础数据模型：`app/common/schemas.py`
- 已创建资产加载模块：`app/common/assets.py`
- 已创建白名单加载模块：`app/common/whitelist.py`
- 已创建事件存储模块：`app/common/event_store.py`
- 已创建 FastAPI 入口：`app/main.py`
- 已创建基础路由：`app/web/routes.py`
- 已创建首页模板：`templates/index.html`

### 1.7 部署基础
- 已创建 systemd 服务模板：`systemd/pi-guard-lite.service`

### 1.8 当前可验证结果
- Python 代码已做语法校验，通过
- 项目已经具备最小可运行骨架
- 数据库初始化能力已具备
- `/health`、`/stats`、`/` 路由代码已具备
- 已新增 `/events` 路由
- 已实现 Suricata 样例文件读取与解析
- 已实现第一版特征提取
- 已实现第一版规则检测
- 已实现第一版风险评分
- 已实现 Suricata 样例流水线入库脚本
- 已补充基础单元测试和样例数据

---

## 2. 正在进行

### 2.1 当前阶段
当前处于：

- **项目骨架完成**
- **感知层第一批基础能力完成**
- **决策层第一批基础能力完成**
- **准备进入执行层开发**

### 2.2 下一步优先事项
1. 接入系统日志输入
2. 实现执行器抽象接口
3. 实现 OpenWrt/Linux SSH 执行器
4. 实现策略生成器
5. 实现 TTL 自动解封

---

## 3. 未完成

## 3.1 树莓派实机相关
以下内容必须等树莓派到货后才能做：

- 烧录 Raspberry Pi OS
- 首次启动
- 树莓派联网
- 树莓派系统更新
- 真机安装 Suricata
- 真机安装 Fluent Bit
- 接交换机镜像口/SPAN
- 真机 systemd 部署验证
- 温度、性能、稳定性调优

## 3.2 感知层
- Suricata 实时 tail 读取与守护进程集成
- syslog/auth/nginx 日志读取器
- Fluent Bit 联动方案
- 原始事件统一入口服务
- 更多日志源结构化解析器
- 原始 JSONL 存储轮转策略

## 3.3 决策层
- 行为基线模块
- AI 模型加载与推理
- 策略生成器

## 3.4 执行层
- 执行器抽象接口
- OpenWrt/Linux SSH 执行器
- 封禁/解封命令落地
- TTL 自动解封
- 人工封禁/解封接口

## 3.5 动态控制层
- HTTP/TCP/Ping 探测
- 策略影响判定
- 自动回滚
- 模型导入与版本管理

## 3.6 Web 层
- 事件列表页
- 策略列表页
- 黑名单页
- 资产页
- 手工操作页
- 通知管理

## 3.7 测试
- 更多单元测试覆盖
- 场景测试
- 误伤测试
- 24 小时稳定性测试

## 3.8 文档与脚本
- 部署文档
- 使用文档
- 排障文档
- 安装脚本
- 启停脚本
- 备份脚本

---

## 4. 当前项目成熟度判断

### 已达到
- 文档设计阶段：**完成**
- 工程初始化阶段：**完成**
- 代码骨架阶段：**完成**

### 尚未达到
- 可运行 MVP：**未完成**
- 真机部署：**未开始**
- 完整闭环：**未完成**

---

## 5. 下一批我要继续做的内容

按优先级，下一批工作顺序如下：

1. `app/collector/` 下实现系统日志读取器
2. `app/executor/` 下实现执行器基类
3. `app/executor/` 下实现 OpenWrt SSH 执行器
4. `app/policy/` 下实现第一版策略生成
5. `app/probe/` 下实现业务探测
6. `app/probe/` 下实现自动回滚

---

## 6. 当前交付文件清单

```text
E:\ctf\开发\pi-guard-lite\
├─ README.md
├─ .gitignore
├─ requirements.txt
├─ app\
│  ├─ collector\
│  │  ├─ raw_store.py
│  │  └─ suricata_reader.py
│  ├─ common\
│  │  ├─ assets.py
│  │  ├─ config.py
│  │  ├─ db.py
│  │  ├─ event_store.py
│  │  ├─ logger.py
│  │  ├─ schemas.py
│  │  └─ whitelist.py
│  ├─ detector\
│  │  └─ rule_engine.py
│  ├─ features\
│  │  └─ extractor.py
│  ├─ main.py
│  ├─ parser\
│  │  └─ suricata_parser.py
│  ├─ scorer\
│  │  └─ risk_scoring.py
│  └─ web\
│     └─ routes.py
├─ config\
│  ├─ app.yaml
│  ├─ assets.yaml
│  ├─ executor.yaml
│  ├─ rules.yaml
│  └─ whitelist.yaml
├─ docs\
│  ├─ 工作状态清单.md
│  └─ 开发规范.md
├─ scripts\
│  ├─ init_db.sql
│  └─ process_suricata_file.py
├─ systemd\
│  └─ pi-guard-lite.service
├─ templates\
│  └─ index.html
└─ tests\
   ├─ __init__.py
   ├─ test_suricata_pipeline.py
   └─ samples\
      └─ suricata_eve.jsonl
```
